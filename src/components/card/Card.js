import React from "react";
import ProfileImage from "../profileImage/ProfileImage";

const Card = props => {
  return (
    <React.Fragment>
      <ProfileImage />
    </React.Fragment>
  );
};

export default Card;
